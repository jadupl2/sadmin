#----------------------------------------------------------------------------
# SADMIN - Configuration file - Created on Mon Dec 14 15:52:38 EST 2015
#   - Comment and blank lines are ignored
#   - The Format is simple (NAME = VALUE)
#   - These are the defaults values used by sadmin tools
#   - Some field can be overriden on a script basis
#        Like SADM_MAIL_TYPE , SADM_MAIL_ADDR overriden at the top of script
#        See the template sadm_template.sh as an example.
#   - If this file (sadmin.cfg) is not present when the 1st script is run,
#     then template file sadmin.org will be copied to sadmin.cfg
#----------------------------------------------------------------------------
#
#
#----------------------------------------------------------------------------
# Email when error and log report are sent
SADM_MAIL_ADDR = root@localhost
#
#
#----------------------------------------------------------------------------
# Put your Company name here
SADM_CIE_NAME = Company_Name
#
#
#----------------------------------------------------------------------------
# Default Option for sending email after a script is ended
# Can be overridden by changing SADM_MAIL_TYPE at the top of template script
# 0 = No Mail Sent      1 = On Error Only
# 2 = On Success Only   3 = Always send email
SADM_MAIL_TYPE = 3
#
#
#----------------------------------------------------------------------------
# SADMIN Server - (MUST be fully qualified name)
SADM_SERVER = nomad.maison.ca
#
#
#----------------------------------------------------------------------------
# This is the user and group that have access to $SADM_BASE_DIR (/sadmin)
# sadm_housekeeping_client.sh and sadm_housekeeping_server.sh will use them
# to make sure all files and directories belong to hem.
SADM_USER = sadmin
SADM_GROUP = sadmin
#
#
#----------------------------------------------------------------------------
SADM_MAX_LOGLINE

#----------------------------------------------------------------------------
SADM_MAX_RCLINE=100

#----------------------------------------------------------------------------
SADM_NMON_KEEPDAYS=40

SADM_SAR_KEEPDAYS=40
